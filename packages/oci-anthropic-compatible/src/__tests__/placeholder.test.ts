import { describe, it, expect } from 'bun:test';

describe('oci-anthropic-compatible', () => {
  it('should have tests', () => {
    // TODO: Add real tests for the Anthropic-compatible proxy
    // This placeholder ensures the test runner doesn't fail with "no tests found"
    expect(true).toBe(true);
  });
});
